spring:
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always
    data: my_test_data.sql
    schema: my_schema.sql

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true